serverFiles:
  prometheus.yml:
    scrape_configs:
      - job_name: "go-api"
        kubernetes_sd_configs:
          - role: endpoints
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            action: keep
            regex: staging-go-api
